---
phase: 73-advantagescope-3d-visualizer-stream
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt
  - marvin19_layout.json
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Swerve module angles are logged in radians instead of degrees"
    - "Odometry covariance matrix diagonals are published to Robot/Odometry/Covariance"
  artifacts:
    - "marvin19_layout.json"
  key_links: []
---

# Phase 73 Plan 01: AdvantageScope 3D Visualizer & Stream

## Objective

- **What:** Configure, optimize, and verify high-fidelity AdvantageScope 3D telemetry streams for the swerve drivetrain, superstructure components, EKF covariance, and active game pieces.
- **Why:** Real-time visual feedback of robot localization, swerve module states, physics-based game pieces, and mechanism articulation is critical for system diagnostics, driver training, and autonomous match verification.
- **Output:** Corrected Swerve radians logging, a new EKF covariance diagonals telemetry stream, and a customized, premium `marvin19_layout.json` at the project root with the version compatibility fix.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Convert Swerve Module Angles to Radians</name>
  <files>
    - frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt
  </files>
  <read_first>
    - frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt
  </read_first>
  <action>
    Modify lines 507-508 in ARESRobot.kt. Convert the computed swerve module angle from degrees to radians by removing the `Math.toDegrees` conversion, passing the raw output of `Math.atan2(wvy, wvx)` directly.
  </action>
  <acceptance_criteria>
    - `ARESRobot.kt` compiles successfully
    - `swerveStates[i * 2] = angle` stores the angle in radians
    - Swerve angle logs in simulator stay bounded within `[-PI, PI]`
  </acceptance_criteria>
  <done>Swerve module angles are logged in radians.</done>
</task>

<task type="code">
  <name>Stream EKF Covariance Diagonals</name>
  <files>
    - frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt
  </files>
  <read_first>
    - frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt
  </read_first>
  <action>
    Add a new telemetry logging line in `ARESRobot.kt`'s `publishTelemetry` method. Extract the diagonals of the EKF covariance matrix (`m00`, `m11`, `m22`) from `currentState.drive.poseEstimator.covariance` and publish them as `Robot/Odometry/Covariance` via `telemetry.putDoubleArray`.
  </action>
  <acceptance_criteria>
    - `ARESRobot.kt` compiles successfully
    - `telemetry.putDoubleArray("Robot/Odometry/Covariance", ...)` publishes the diagonal array `[m00, m11, m22]`
    - Covariance values are observable in real time in the simulation logs
  </acceptance_criteria>
  <done>EKF covariance diagonals are logged under Robot/Odometry/Covariance.</done>
</task>

<task type="code">
  <name>Create Premium AdvantageScope Layout</name>
  <files>
    - marvin19_layout.json
  </files>
  <read_first>
    - .planning/phases/73-advantagescope-3d-visualizer-stream/73-CONTEXT.md
  </read_first>
  <action>
    Create a new AdvantageScope layout file named `marvin19_layout.json` at the workspace root. Define:
    1. A 3D Field tab containing the robot chassis, Superstructure mechanism subcomponents (Intake, Cowl, Flywheel), and Fuel game pieces poses.
    2. A Swerve tab mapping target and actual states.
    3. A Line Graph tab tracking Odometry covariance diagonals (X, Y, Theta variance) and superstructure RPM/Angles.
    Explicitly append the `"version": "26.0.0"` root attribute to guarantee format compatibility.
  </action>
  <acceptance_criteria>
    - `marvin19_layout.json` exists in workspace root
    - Root level contains `"version": "26.0.0"`
    - JSON is syntactically valid and contains configurations for the 3D Field, Swerve, and Line Graph tabs
  </acceptance_criteria>
  <done>AdvantageScope layout JSON is written to root and contains the version 26.0.0 fix.</done>
</task>

## Verification

- Run a full clean build to verify compilation: `.\gradlew.bat build`
- Launch the simulator, run teleop or auto and verify telemetry updates:
  - Swerve module states show radians.
  - Odometry covariance is streamed correctly.
- Import `marvin19_layout.json` in AdvantageScope to verify layout configuration.

## Success Criteria

- [ ] Swerve module logging publishes angles in radians.
- [ ] Odometry covariance diagonals array is published successfully.
- [ ] `marvin19_layout.json` generated in workspace root with `"version": "26.0.0"`.
