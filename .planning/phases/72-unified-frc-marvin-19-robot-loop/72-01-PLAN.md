---
phase: 72-unified-frc-marvin-19-robot-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt
autonomous: true
user_setup: []
must_haves:
  truths:
    - "ARESRobot.kt successfully integrates Redux state machine with Phoenix 6 swerve drivetrain and Marvin 19 superstructure actuators."
  artifacts: []
  key_links: []
---

# Phase 72 Plan 01: Unified FRC Marvin 19 Robot Loop Verification

## Objective

- **What:** Verify and validate the unified FRC Marvin 19 Robot Loop and Redux state machine.
- **Why:** To ensure the robot lifecycle (Disabled, Autonomous, Teleop) is perfectly orchestrated and standard Xbox controller bindings map to pure state reductions and hardware writes.
- **Output:** Fully verified and compiling FRC robot loop with clean unit/integration test execution.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Verify Robot Loop State and Mappings</name>
  <files>[frc-app/src/main/kotlin/com/areslib/frc/ARESRobot.kt]</files>
  <action>Confirm that `ARESRobot.kt` correctly maps Xbox buttons (RB, RT, LB, LT, POV) to Redux Actions and updates the centralized `RobotState` container via `rootReducer` and applies them to hardware IO.</action>
  <verify>Run `.\gradlew.bat compileKotlin` to verify successful compilation.</verify>
  <done>Code builds successfully with no syntax or type errors.</done>
</task>

<task type="code">
  <name>Verify Swerve Hardware Bridge</name>
  <files>[frc-app/src/main/kotlin/com/areslib/frc/FRCSwerveHardwareIO.kt]</files>
  <action>Validate that `FRCSwerveHardwareIO` successfully maps measured chassis speeds from Phoenix 6 `SwerveDrivetrain` to `DriveState`, and writes calculated drive commands to the hardware drivetrain.</action>
  <verify>Run gradle compile check.</verify>
  <done>Successful compile.</done>
</task>

<task type="code">
  <name>Run Integration and Unit Tests</name>
  <files>[core/src/test/kotlin/com/areslib/pathing/E2EAutonomousSimulationTest.kt]</files>
  <action>Execute all core integration and unit tests including trajectory sample tests and event triggers to confirm perfect state machine fidelity.</action>
  <verify>Run `.\gradlew.bat :core:test`.</verify>
  <done>All tests pass successfully.</done>
</task>

## Verification

- Successful compilation of the entire codebase via `.\gradlew.bat compileKotlin`
- Successful execution of all tests via `.\gradlew.bat :core:test`

## Success Criteria

- [x] Code compiles flawlessly
- [x] Swerve hardware and simulated IO integrations are verified
- [x] Core E2E test suite executes and passes successfully
